#include "bishop.h"
using namespace std;

bool Bishop::isValid()
{
  string current = getPosition();
  if (getColor=='w')
  {
	
  }

  else if (getColor == 'b')
  {

  }
}

void Bishop::Move(const string posiiton)
{
  //Check if valid, then:
  setPosition(position);
}
