#include "king.h"
using namespace std;

bool King::isValid()
{
  string current = getPosition();
  if (getColor=='w')
  {
	
  }

  else if (getColor == 'b')
  {
	
  }
}

void King::Move(const string posiiton)
{
  //Check if valid, then:
  setPosition(position);
}
